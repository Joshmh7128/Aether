draw_self()
/*
if distance_to_point(global.playerx,global.playery) < 1000
{
	laser_cast(x,y,direction);
}

if distance_to_point(global.playerx,global.playery) < 1000
{
	laser_cast(x,y,direction+90);
}

if distance_to_point(global.playerx,global.playery) < 1000
{
	laser_cast(x,y,direction+180);
}

if distance_to_point(global.playerx,global.playery) < 1000
{
	laser_cast(x,y,direction+270);
}